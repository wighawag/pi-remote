---
"wherever-vscode": minor
---

Implement the Wherever VS Code Companion extension. This features an IDE-native Sidebar Chat GUI that integrates seamlessly with the local standalone server, utilizes `@wherever-dev/client` for state and communication management, and coordinates directly with the VS Code extension host to trigger deep editor actions (like opening documents or side-by-side git diff views).
