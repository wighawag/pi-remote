---
"wherever-dev": patch
"@wherever-dev/pi": patch
---

Rename project from "Pi Remote" to "Wherever" with domain "wherever.dev". Update npm packages to "wherever-dev" and "@wherever-dev/pi", refactor code references, state stores, configuration directories to `~/.wherever`, and update documentation.
